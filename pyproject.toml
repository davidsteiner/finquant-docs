[tool.poetry]
name = "finquant-docs"
version = "0.1.0"
description = "Dependencies for finquant.rs"
authors = ["David Steiner"]

[tool.poetry.dependencies]
python = "~3.10"
mkdocs-material = "^9.4.6"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
